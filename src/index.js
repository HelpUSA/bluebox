import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <div className="text-2xl text-center text-blue-600 mt-10">
      🚗 BlueBox - Site em construção!
    </div>
  </React.StrictMode>
);